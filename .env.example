# Credenciales de PostgreSQL
# Estas variables son utilizadas por los servicios de n8n y postgres.
# Cambia la contraseña por una segura.
POSTGRES_USER=n8n_user
POSTGRES_PASSWORD=change-this-strong-password
POSTGRES_DB=n8n_database

# Configuración de n8n
# El host y la URL del webhook deben actualizarse si expones n8n públicamente,
# por ejemplo, detrás de un proxy inverso.
N8N_HOST=localhost
WEBHOOK_URL=http://localhost:5678/

# Zona Horaria
# Encuentra tu zona horaria en la lista de la base de datos TZ (ej: America/New_York, Europe/London).
GENERIC_TIMEZONE=America/Bogota

# Credenciales de Qdrant
# Opcional: Define una clave de API para asegurar tu instancia de Qdrant.
# Si se define, esta clave deberá ser enviada en las cabeceras de las peticiones (`api-key: <tu-clave>`).
QDRANT_API_KEY=your-secret-api-key
